services:
  server:
    build: ./test-sps-server
    container_name: sps-server
    ports:
      - "3333:3333"
    environment:
      - PORT=3333
    volumes:
      - ./test-sps-server:/app
      - /app/node_modules
    command: yarn dev
